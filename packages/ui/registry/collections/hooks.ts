/**
 * Hooks components registry collection
 * Maps cuicui hooks to shadcn registry format
 */

import type { RegistryItem } from "../schema";

/**
 * Hooks registry items collection
 */
export const hooksRegistryItems: RegistryItem[] = [
  // TODO: Map actual hooks components here
];