import type { ComponentMetaType } from "@/lib/types/component";

export const Component: ComponentMetaType = {
  name: "Trailing Images",
  description:
    "A component that creates a trailing image effect following the cursor, adding a dynamic and interactive visual experience to your project.",
};

export default Component;
