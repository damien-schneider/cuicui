import type { ComponentMetaType } from "@/lib/types/component";

export const Component: ComponentMetaType = {
  name: "ClassNames",
  description:
    "A utils function to generate class names working with conditional classes",
  sizePreview: "md",
};

export default Component;
