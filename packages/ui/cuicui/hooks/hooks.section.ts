import type { SectionMetaType } from "@/lib/types/component";

import { ToyBrickIcon } from "lucide-react";

export const hooksSection: SectionMetaType = {
  name: "Hooks",
  description: "A collection of React hooks for building modern applications.",
  icon: ToyBrickIcon,
};

export default hooksSection;
