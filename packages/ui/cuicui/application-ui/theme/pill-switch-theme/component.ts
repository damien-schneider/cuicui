import type { ComponentMetaType } from "@/lib/types/component";

export const Component: ComponentMetaType = {
  name: "CornerSwitchTheme",
  description: "A switch theme component with a corner gradient",
  isIframed: true,
};

export default Component;
