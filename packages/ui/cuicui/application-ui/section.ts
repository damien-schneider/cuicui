import { AppWindowMacIcon } from "lucide-react";
import type { SectionMetaType } from "@/lib/types/component";

export const applicationUiSection: SectionMetaType = {
  name: "Application UI",
  description: "A collection of UI components for advanced applications.",
  icon: AppWindowMacIcon,
};

export default applicationUiSection;
