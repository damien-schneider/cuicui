import type { ComponentMetaType } from "@/lib/types/component";

export const batteryIndicator: ComponentMetaType = {
  sizePreview: "sm",
  name: "Battery Indicator",
  description: "A battery indicator component.",
};

export default batteryIndicator;
