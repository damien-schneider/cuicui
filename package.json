{
  "private": true,
  "scripts": {
    "pre-build": "turbo pre-build",
    "build": "pnpm migrate && pnpm validate && tsc && pnpm generate-section-list",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "start": "turbo start",
    "type-check": "turbo type-check",
    "clean": "turbo clean",
    "test": "turbo run test",
    "test:watch": "turbo run test:watch",
    "migrate": "ts-node scripts/migrate-to-json.ts",
    "validate": "ts-node scripts/validate-components.ts",
    "generate-section-list": "ts-node scripts/generate-section-list/index.ts"
  },
  "devDependencies": {
    "@biomejs/biome": "1.9.2",
    "@vitejs/plugin-react": "4.3.1",
    "turbo": "^2.3.3",
    "vitest": "2.0.5",
    "@types/jsonschema": "^1.1.4",
    "@types/node": "^20.11.24",
    "glob": "^10.3.10",
    "jsonschema": "^1.4.1",
    "ts-node": "^10.9.2"
  },
  "packageManager": "pnpm@9.0.4",
  "engines": {
    "node": ">=18"
  },
  "name": "cuicui"
}
